from setuptools import setup

setup(
    name='pyequa',
    version='0.1.0',
    description='Equations for all',
    author='Pedro Cruz',
    packages=['pyequa'] #, 'my_package.data_processing'],  # Include all packages
)


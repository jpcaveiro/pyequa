---
title: "dados-study.Rmd"
author: "Pedro Cruz"
date: "2025-01-31_07-11-00"
output:
  html_document: default
---


# Model dados-study.Rmd - CLOZE



## variante 001 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média (ou valor esperado) da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 002 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 003 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 004 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 005 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 006 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 007 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 008 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 009 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 010 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 011 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 012 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 013 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 014 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 015 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 016 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 017 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 018 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 019 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 020 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 021 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 022 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 023 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 024 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 025 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 026 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 027 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 028 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 029 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 030 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 031 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 032 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 033 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 034 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 035 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 036 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 037 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 038 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 039 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 040 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 041 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 042 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 043 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 044 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 045 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 046 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 047 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 048 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 049 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 050 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 051 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 052 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 053 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 054 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 055 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 056 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 057 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 058 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 059 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 060 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 061 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 062 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 063 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 064 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 065 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 066 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 067 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 068 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 069 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 070 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 071 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 072 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 073 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 074 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 075 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 076 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 077 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 078 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 079 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 080 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 081 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 082 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 083 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 084 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 085 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 086 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 087 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 088 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 089 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 090 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 091 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 092 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 093 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 094 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 095 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 096 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 097 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 098 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 099 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 100 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 101 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 102 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 103 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 104 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 105 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 106 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 107 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 108 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 109 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 110 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 111 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 112 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 113 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 114 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 115 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 116 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 117 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 118 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 119 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 120 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 121 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 122 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 123 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 124 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 125 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 126 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 127 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 128 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 129 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 130 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 131 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 132 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 133 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 134 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 135 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 136 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 137 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 138 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 139 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 140 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 141 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 142 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 143 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 144 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 145 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 146 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 147 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 148 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 149 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 150 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 151 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 152 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 153 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 154 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 155 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 156 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 157 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 158 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 159 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 160 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 161 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 162 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 163 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 164 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 165 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 166 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 167 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 168 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 169 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 170 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 171 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 172 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 173 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 174 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 175 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 176 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 177 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 178 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 179 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 180 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 181 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 182 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 183 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 184 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 185 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 186 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 187 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 188 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 189 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 190 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 191 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 192 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 193 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 194 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 195 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 196 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 197 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 198 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 199 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 200 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 201 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 202 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 203 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 204 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 205 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 206 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 207 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 208 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 209 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 210 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 211 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 212 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 213 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 214 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 215 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 216 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 217 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 218 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 219 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 220 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 221 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 222 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 223 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 224 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 225 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 226 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 227 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 228 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 229 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 230 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 231 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 232 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 233 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 234 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 235 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 236 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 237 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 238 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 239 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 240 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 241 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 242 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 243 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 244 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 245 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 246 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 247 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 248 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 249 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 250 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 251 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 252 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 253 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 254 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 255 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 256 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 257 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 258 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 259 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 260 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 261 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 262 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 263 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 264 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 265 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 266 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 267 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 268 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 269 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 270 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 271 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 272 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 273 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 274 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 275 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 276 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 277 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 278 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 279 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 280 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 281 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 282 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 283 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 284 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 285 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 286 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 287 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 288 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 289 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 290 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 291 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 292 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 293 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 294 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 295 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 296 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 297 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 298 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 299 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 300 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 301 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 302 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 303 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 304 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 305 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 306 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 307 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 308 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 309 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 310 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 311 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 312 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 313 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 314 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 315 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 316 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 317 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 318 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 319 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 320 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 321 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 322 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 323 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 324 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 325 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 326 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 327 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 328 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 329 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 330 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 331 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 332 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 333 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 334 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 335 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 336 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 337 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 338 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 339 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 340 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 341 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 342 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 343 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 344 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 345 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 346 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 347 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 348 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 349 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 350 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 351 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 352 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 353 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 354 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 355 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 356 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 357 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 358 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 359 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 360 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 361 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 362 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 363 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 364 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 365 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 366 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 367 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 368 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 369 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 370 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 371 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 372 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 373 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 374 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 375 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 376 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 377 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 378 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 379 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 380 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 381 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 382 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 383 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 384 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 385 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 386 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 387 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 388 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 389 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 390 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 391 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 392 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 393 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 394 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 395 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 396 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 397 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 398 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 399 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 400 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 401 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 402 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 403 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 404 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 405 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 406 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 407 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 408 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 409 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 410 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 411 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 412 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 413 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 414 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 415 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 416 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 417 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 418 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 419 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 420 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 421 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 422 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 423 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 424 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 425 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 426 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 427 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 428 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 429 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 430 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 431 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 432 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 433 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 434 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 435 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 436 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 437 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 438 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 439 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 440 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 441 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 442 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 443 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 444 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 445 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 446 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 447 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 448 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 449 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 450 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 451 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 452 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 453 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 454 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 455 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 456 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 457 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 458 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 459 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 460 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 461 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 462 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 463 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 464 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 465 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 466 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 467 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 468 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 469 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 470 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 471 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 472 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 473 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 474 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 475 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 476 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 477 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 478 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 479 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 480 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 481 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 482 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 483 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 484 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 485 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 486 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 487 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 488 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 489 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 490 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 491 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 492 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 493 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 494 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 495 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 496 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 497 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 498 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 499 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 500 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 501 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 502 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 503 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 504 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 505 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 506 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 507 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 508 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 509 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 510 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 511 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 512 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 513 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 514 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 515 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 516 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 517 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 518 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 519 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 520 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 521 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 522 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 523 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 524 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 525 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 526 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 527 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 528 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 529 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 530 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 531 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 532 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 533 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 534 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 535 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 536 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 537 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 538 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 539 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 540 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 541 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 542 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 543 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 544 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 545 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 546 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 547 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 548 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 549 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 550 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 551 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 552 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 553 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 554 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 555 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 556 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 557 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 558 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 559 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 560 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 561 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 562 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 563 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 564 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 565 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 566 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 567 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 568 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 569 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 570 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 571 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 572 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 573 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 574 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 575 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 576 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 577 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 578 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 579 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 580 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 581 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 582 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 583 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 584 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 585 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 586 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 587 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 588 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 589 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 590 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 591 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 592 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 593 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 594 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 595 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 596 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 597 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 598 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 599 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 600 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 601 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 602 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 603 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 604 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 605 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 606 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 607 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 608 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 609 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 610 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 611 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 612 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 613 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 614 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 615 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 616 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 617 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 618 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 619 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 620 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 621 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 622 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 623 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 624 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 625 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 626 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 627 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 628 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 629 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 630 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 631 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 632 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 633 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 634 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 635 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 636 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 637 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 638 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 639 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 640 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 641 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 642 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 643 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 644 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 645 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 646 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 647 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 648 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 649 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 650 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 651 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 652 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 653 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 654 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 655 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 656 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 657 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 658 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 659 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 660 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 661 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 662 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 663 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 664 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 665 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 666 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 667 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 668 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 669 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 670 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 671 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 672 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 673 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 674 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 675 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 676 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 677 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 678 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 679 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 680 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 681 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 682 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 683 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 684 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 685 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 686 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 687 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 688 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 689 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 690 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 691 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 692 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 693 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 694 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 695 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 696 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 697 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 698 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 699 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 700 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 701 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 702 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 703 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 704 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 705 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 706 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 707 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 708 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 709 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 710 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 711 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 712 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 713 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 714 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 715 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 716 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 717 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 718 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 719 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 720 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 721 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 722 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 723 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 724 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 725 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 726 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 727 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 728 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 729 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 730 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 731 (data row is 03)

A probabilidade de acerto no alvo é p=**0.2**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 732 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 733 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 734 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 735 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 736 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 737 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 738 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 739 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 740 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 741 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 742 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 743 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 744 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 745 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 746 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 747 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 748 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 749 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 750 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 751 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 752 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 753 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 754 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 755 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 756 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 757 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 758 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 759 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 760 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **6.3**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 761 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 762 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 763 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 764 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 765 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 766 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 767 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 768 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 769 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 770 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 771 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 772 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 773 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 774 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 775 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 776 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 777 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 778 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 779 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 780 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 781 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 782 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 783 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 784 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 785 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 786 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 787 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 788 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 789 (data row is 05)

A probabilidade de acerto no alvo é p=**0.7**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 790 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**25** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 791 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 792 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 793 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 794 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 795 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 796 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 797 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 798 (data row is 06)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 799 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 800 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 801 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 802 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**20** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 803 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 804 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 805 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 806 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 807 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 808 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 809 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = **0.8237135152559875**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 810 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = **4.800000000000001**

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 811 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 812 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**10** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 813 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.0**
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 814 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **7.5**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 815 (data row is 07)

A probabilidade de acerto no alvo é p=**0.4**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **8.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 816 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **10** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **9.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **10** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 817 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **20** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **20** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7719717298748721:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 818 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **14** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **14** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 819 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **1.6**

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 820 (data row is 04)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.0999999999999996**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 821 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 822 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 823 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **5** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a **5** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 824 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 825 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n=**30** tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 826 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = **0.8744010272769627**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.8193447857369106:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 827 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = **0.8791261184**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 828 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = **0.8497316674**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 829 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = **0.8506916541**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 830 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 831 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 832 (data row is 08)

A probabilidade de acerto no alvo é p=**0.3**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 833 (data row is 01)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 834 (data row is 02)

A probabilidade de acerto no alvo é p=**0.6**.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 835 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **3** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%2.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **3** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7854023497798255:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 836 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **4** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%3.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **4** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920876:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **não se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 837 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a **8** | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.1000000000000005:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a **8** | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.7549235197920875:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 838 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.19348844208806504**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%5.25:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.13761676203741716:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 839 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = **0.12559897272303736**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.8:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.08545176011539872:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 840 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = **0.7303703863027229**.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = {:NUMERICAL:%100%0.6548358352679033:0.01}.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 841 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **18.0**
* variância da Normal é Var[X] = np(1-p) = **7.2**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 842 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **12.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 843 (data row is 03)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.2:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8791261184:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **2.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%1.6:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%3:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7854023497798255**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 844 (data row is 04)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8497316674:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = **3.0**
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%2.0999999999999996:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%4:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920876**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 845 (data row is 05)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.7:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%10:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8506916541:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **2.1000000000000005**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%8:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7549235197920875**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica},
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 846 (data row is 06)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%25:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.19348844208806504:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%7.5:0.01}
* variância da Normal é Var[X] = np(1-p) = **5.25**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.13761676203741716**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 847 (data row is 07)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.4:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.12559897272303736:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%8.0:0.01}
* variância da Normal é Var[X] = np(1-p) = **4.8**

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%5:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.08545176011539872**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%0%se verifica\~%100%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 848 (data row is 08)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.3:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.7303703863027229:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%9.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%6.3:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%10:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.6548358352679033**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica},
* \(n \ge 30 \) que **se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 849 (data row is 01)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%30:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8237135152559875:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%18.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%7.2:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%20:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.7719717298748721**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que **se verifica**, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **se verifica**,
* \(n \ge 30 \) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica} (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)




## variante 850 (data row is 02)

A probabilidade de acerto no alvo é p={:NUMERICAL:%100%0.6:0.01}.

A variável aleatória

X = "número de acertos no tiro ao alvo em n={:NUMERICAL:%100%20:0.01} tiros"

é modelada por uma distribuição Binomial(n,p). 

Então,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim \text{Binomial}\)) = {:NUMERICAL:%100%0.8744010272769627:0.01}.

O conceito seguinte têm aplicação no Capítulo 3.

Uma Binomial é uma soma de variáveis 0 ou 1 (contagem de sucessos). Então, usando o TLC, uma distribuição Binomial(n,p) aproxima-se a uma distribuição Normal com os seguintes parâmetros: Normal(np, np(1-p)):

* a média da Normal é E[X] = np = {:NUMERICAL:%100%12.0:0.01}
* variância da Normal é Var[X] = np(1-p) = {:NUMERICAL:%100%4.800000000000001:0.01}

Usando esta distribuição aproximada,

P(X menor ou igual a {:NUMERICAL:%100%14:0.01} | \(X \sim_\text{aprox.} \text{Normal}\)) = **0.8193447857369106**.

Costuma-se usar a seguintes regras que sugerem que a probabilidades calculadas pela Binomial e pela Normal são próximas:

* média da Binomial = \(np \ge 5\) que {:MULTICHOICE_S:%100%se verifica\~%0%não se verifica}, e,
* variância da Binomial = \(np(1-p) \ge 5\) que **não se verifica**,
* \(n \ge 30 \) que **não se verifica** (regra usual do TLC),



### feedback



#(Consulte o docente das suas turmas ou uma OT.)



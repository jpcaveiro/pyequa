[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pyequa"
version = "0.0.1"
dependencies = [
    "sympy",
    "pandas",
    "networkx",
    "matplotlib",
    "openpyxl",
    "PyYAML",
]
